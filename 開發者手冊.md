# dyagent 開發者手冊

## 1. 系統概覽

dyagent 是一套以 FastAPI + 前端 Vite 為核心的多代理（Agent）系統，支援：
- Agent 管理與權限控管
- Router-Worker 聊天分派
- MCP / Skills / Functions 能力治理
- RAG 公有與私有資料集
- 聊天事件與路由事件審計

專案主要結構：

```text
backend/
frontend/
specs/
```

---

## 2. 模組架構

### 2.1 Backend 架構

- 入口：`backend/src/api/main.py`
  - 掛載所有 API router
  - 啟動時連線 Redis/Qdrant，並做必要 seed（例如主代理）
  - Schema 變更採 Alembic（Alembic-only）

- 路由層：`backend/src/api/routes/*`
  - `auth.py`：登入註冊
  - `agents.py`：代理者 CRUD、整合設定、RAG/MCP 測試
  - `chat.py`：聊天 SSE、`/api/chat` 統一入口、路由事件
  - `rag.py`：文件上傳、資料集管理
  - `functions_admin.py`：Functions 全域管理
  - `mcps_admin.py` / `skills_admin.py`：MCP/Skills 管理

- 服務層：`backend/src/services/*`
  - `chat_router.py`：工具執行、整合上下文
  - `llm_client.py`：LLM 路由與串流
  - `embedding_service.py`：embedding provider 切換（sentence_transformers/ollama/vllm）
  - `qdrant_service.py`：向量庫操作
  - `rag_vectorizer.py`：切塊與回退向量化

- 資料層：`backend/src/models/*`
  - 核心實體：`Agent`、`Conversation`、`Message`、`Document`
  - 治理實體：`FunctionProfile`、`RagDataset`、`LlmTurn`
  - 事件實體：`EventPart`（tool/routing events）

### 2.2 Frontend 架構

- 公用入口：`frontend/src/main.js`
  - API client、auth、共用 UI helper
  - 依角色注入導覽列

- 頁面：`frontend/src/pages/*.html`
  - `chat.html`：聊天主頁（一般使用者自動路由）
  - `agent-llm-config.html`：代理進階設定（Functions/RAG 綁定）
  - `functions.html`：Functions 管理
  - `rag-datasets.html`：RAG 公有資料集管理

---

## 3. 權限模型

定義在 `backend/src/middleware/rbac.py`：

- `admin`
  - Agent/User/Logs 全權、chat
  - Functions 建立/編輯/刪除
  - RAG 公有資料集管理

- `agent_admin`
  - 可讀/更新 agent、chat
  - 可在 agent 設定頁綁定 Functions、建立私有資料集

- `user`
  - chat
  - 聊天頁不需手動選代理者，直接走 Router

---

## 4. Router-Worker 與聊天流程

### 4.1 統一入口
- Endpoint：`POST /api/chat`
- 行為：主代理先收訊息，分派 worker，再透過 SSE 回傳

### 4.2 路由策略（混合）
1. 規則匹配（名稱/描述關鍵字）
2. embedding 相似度匹配（門檻：`ROUTER_EMBEDDING_THRESHOLD`）
3. LLM 裁決
4. fallback 到預設 worker

### 4.3 路由可觀測性
- SSE 會送出 `route.decision`
- DB 事件可查：
  - `GET /api/conversations/{conversation_id}/routing-events`

---

## 4a. 聊天模式詳細架構（ReAct + SSE 串流）

### 4a.1 入口端點

```
GET /api/agents/{agent_id}/chat/stream
```

使用 SSE（Server-Sent Events）長連線，LLM 輸出逐字增量推送至前端。

### 4a.2 每輪對話流程

```
用戶訊息
    ↓
1. 查找/建立 Conversation
2. 儲存 user Message
3. 組 Prompt（加入能力前綴：skills / mcp / RAG 資訊）
    ↓
LLM 串流輸出（逐字增量 SSE）
    ↓
邊串流邊掃描 [[CALL tool=XXX]] 語法
    ├─ 無工具呼叫 → 直接輸出文字，done
    └─ 偵測到工具呼叫
           ↓
       ReAct 循環（最多 REACT_MAX_STEPS 步，預設 3）
           ↓
       依工具類型執行：
       ├─ mcp:* + stdio   → subprocess stdio JSON-RPC 串流
       ├─ mcp:* + base_url → WebSocket JSON-RPC 串流
       └─ skill / function → call_tool_async（HTTP / 本地）
           ↓
       工具結果回餵 LLM → 繼續輸出
           ↓
       失敗/超時 → fallback（LLM 用一般知識回覆）
```

### 4a.3 SSE 事件類型

| 事件 `type`   | 說明 |
|---------------|------|
| `text`        | LLM 文字增量（`delta` 欄位）|
| `react`       | ReAct 階段通知（`phase`：`plan` / `act_start` / `act_result` / `reroute` / `finish`）|
| `tool_start`  | 工具開始執行（含 `args`，前端可用於重執行）|
| `tool`        | 工具回傳 frame（`name` + `frame` 欄位）|
| `progress`    | 工具執行進度（`value` 0~100，可選 `eta_seconds`）|
| `heartbeat`   | 每 10 秒保活心跳（`ts` 欄位）|
| `done`        | 本輪結束（含 `conversation_id`）|

### 4a.4 工具呼叫語法

LLM 輸出以下格式即觸發工具執行：

```
[[CALL tool=<tool_name>]]
{"key": "value", ...}
```

- `tool_name` 格式：
  - `mcp:<connection_name>`（走 MCP 連線）
  - `skill:<skill_name>`（走 Skills 執行器）
  - `fn:<function_name>`（走 Function 本地呼叫）

### 4a.5 ReAct 循環上限與失敗處理

| 情況 | 行為 |
|------|------|
| 步驟數超過 `REACT_MAX_STEPS`（預設 3）| 停止執行，回傳錯誤提示 |
| MCP 工具逾時（`MCP_TOOL_TIMEOUT_SEC`，預設 30s）| 觸發 `reroute`，改以 LLM 一般知識回覆 |
| 工具回傳 `ok: false` | 觸發 `reroute`，改以 LLM 一般知識回覆 |
| LLM 路由不可用（`LLM_HARD_FAIL_ON_NO_ROUTE=true`）| 提前終止，回傳 `模型路由暫時不可用` |

### 4a.6 Router Agent 選路（三層策略）

當 Agent 是 Router 類型時，收到訊息後先選路到 Worker：

1. **規則匹配**：訊息含 Worker 名稱或描述關鍵字 → 直接分派
2. **Embedding 相似度**：訊息向量 vs Worker 描述向量，得分 ≥ `ROUTER_EMBEDDING_THRESHOLD`（預設 0.55）→ 分派
3. **LLM 裁決**：前兩層信心不足時，由 LLM 從候選清單選出最適 Worker
4. **Fallback**：三層皆無結果 → 分派給第一個 Worker

---

## 5. Functions / RAG 治理模型

### 5.1 Functions
- 全域管理：`/api/functions`（admin）
- 代理綁定：`PUT /api/agents/{id}/function-profile`
- 進階覆寫優先序：
  1) agent 的 `functions_definition_template`
  2) 綁定 profile 模板
  3) 系統預設

### 5.2 RAG
- 公有資料集：`scope=global`
  - 建立：`POST /api/rag/datasets`
  - 上傳：`POST /api/rag/datasets/{dataset_id}/upload`
- 私有資料集：`scope=agent_private`
  - 建立：`POST /api/agents/{agent_id}/rag/datasets`
  - 只能綁定同 agent

文件狀態：`uploaded/indexing/ready/failed`

---

## 6. API 調用方式

### 6.1 認證
先登入取 token：

```bash
curl -X POST http://127.0.0.1:8000/api/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin"}'
```

後續都帶：

```text
Authorization: Bearer <token>
```

### 6.2 一般聊天（統一入口）

```bash
curl -N -X POST http://127.0.0.1:8000/api/chat \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"message":"請幫我查本月營收"}'
```

### 6.3 建立 Function Profile（admin）

```bash
curl -X POST http://127.0.0.1:8000/api/functions \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "name":"generic-strict",
    "provider":"openai",
    "function_definition_template":"[[CALL tool=<tool_name>]]\\n{}",
    "enabled":true
  }'
```

### 6.4 匯入 Claude Skill（admin）

解析 Claude Skill 內容並建立 Function Profile：

```bash
# 僅解析預覽（不建立）
curl -X POST http://127.0.0.1:8000/api/functions/import-claude-skill \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "content": "---\nname: code-review\ndescription: Review code\nprompt: |\n  Review the code...\n---"
  }'
# 回應：{ "ok": true, "parsed": { "name": "...", "description": "...", "template": "..." } }

# 解析並建立 Function Profile
curl -X POST http://127.0.0.1:8000/api/functions/import-claude-skill \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{
    "content": "---\nname: code-review\ndescription: Review code\nprompt: |\n  Review the code...\n---",
    "create": true
  }'
# 回應：{ "ok": true, "parsed": {...}, "created": {...} }
```

支援格式：
- YAML front matter（含 `name`/`description`/`prompt` 欄位）
- 純 Markdown（`# 標題` 作為名稱，其餘為 template）

### 6.5 綁定 Agent Function Profile

```bash
curl -X PUT http://127.0.0.1:8000/api/agents/<agent_id>/function-profile \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"function_profile_id":"<profile_id>"}'
```

### 6.6 建立公有 RAG 資料集

```bash
curl -X POST http://127.0.0.1:8000/api/rag/datasets \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"name":"company-kb","scope":"global","enabled":true}'
```

### 6.7 上傳公有資料集文件

```bash
curl -X POST "http://127.0.0.1:8000/api/rag/datasets/<dataset_id>/upload" \
  -H "Authorization: Bearer <token>" \
  -F "file=@./docs/faq.txt"
```

---

## 7. 設定重點（.env）

在 `backend/.env`：

```env
EMBEDDING_PROVIDER=sentence_transformers
EMBEDDING_MODEL_NAME=BAAI/bge-m3
ROUTER_EMBEDDING_THRESHOLD=0.55
```

可選 provider：
- `deterministic`
- `sentence_transformers`
- `ollama`
- `vllm`

---

## 8. 開發流程建議

1. 先跑 migration：`cd backend && alembic upgrade head`
2. 啟動服務：`./start-dev.sh`
3. 開發後驗證：
   - Backend：`pytest -q`
   - Frontend：`cd frontend && npm run build`
4. 觀察 routing 與工具事件，優先看 `routing-events` 與 SSE

---

## 9. 常見除錯點

- 聊天回 `模型路由不可用`
  - 檢查 LLM provider 設定與 API key
  - 檢查對應模型服務連線

- RAG 上傳後查不到
  - 確認文件狀態為 `ready`
  - 確認 agent 綁定了正確資料集
  - 確認 Qdrant collection 有資料

- 頁面 404
  - 確認 `frontend/vite.config.js` 已加入對應頁面 entry
  - 重新 `npm run build` 並重啟前端
