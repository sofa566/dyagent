3. 如何判斷單代理 vs 多代理路徑
三層判斷，依序過濾：


第一層：快速排除（不呼叫 LLM）
─────────────────────────────
  訊息 < 15 字  →  單代理
  可用 workers < 2  →  單代理
  沒有任何複合意圖信號  →  單代理

  複合意圖信號（任一符合）：
  - 複合連接詞：並且/同時/另外/然後/以及/也要/and then/also
  - 多個動詞+不同受詞（簡單斷句計數）
  - 訊息含明確 @代理者名稱

第二層：Embedding 能力距離（不呼叫 LLM）
─────────────────────────────────────────
  計算訊息向量與每個 Worker 描述向量的餘弦相似度
  若「前兩高得分 Worker」的得分差 < 0.1（即兩者都很相關）
  → 可能需要多代理，進入第三層

  若最高得分 Worker 遠超其他（差 > 0.2）
  → 單代理（那個 Worker）

第三層：LLM 裁決（必要時才呼叫，費用最高）
────────────────────────────────────────────
  提示：「你是任務分解器。判斷此請求需要幾個不同專長的代理者...」
  回傳：{ "multi": true/false, "tasks": [...] }
  解析失敗  →  降級單代理
  multi=false  →  單代理
  tasks 全部 agent_id 相同  →  單代理
  tasks 含 2+ 不同 agent_id  →  多代理路徑
判斷流程圖：


訊息進入
  ↓
快速排除？ → 是 → 單代理（原路徑）
  ↓ 否
Embedding 差距明顯？ → 是 → 單代理（最高分 Worker）
  ↓ 否（兩者相近）
LLM 裁決 multi=true？ → 否 → 單代理
  ↓ 是
多代理 Orchestrator 路徑
現在我來更新計畫檔案，把以上三點都納入：
計畫已更新。整理一下與你討論的三個確認點：

1. 合成 + ReAct 重試
所有子代理跑完 → Router LLM 合成 → Router LLM 自評 → 不過就重新分解再跑，最多 max_steps 輪。這才是完整的 ReAct。

2. DB 正規化
新增兩張專屬表 multi_agent_sessions 和 multi_agent_tasks，有正規的 FK 和 status 欄位。EventPart 繼續做可觀測性紀錄，兩者職責分離。

3. 判斷路徑的三層

第一層（零成本）：短訊息、Worker 不夠、無複合信號 → 直接單代理
第二層（embedding，無 LLM 費用）：最高分 Worker 明顯勝出 → 單代理；兩個 Worker 得分相近 → 進第三層
第三層（LLM 裁決，有成本）：只在確實模稜兩可時才呼叫

