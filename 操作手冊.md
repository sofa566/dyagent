# dyagent 操作手冊

## 1. 環境與啟動

### 1.1 必要條件
- Python 3.11+
- Node.js（前端 Vite）
- Docker / Docker Compose（PostgreSQL、Redis、Qdrant）

### 1.2 啟動開發環境
在專案根目錄執行：

```bash
./start-dev.sh
```

啟動後預設網址：
- Backend: `http://127.0.0.1:8000`
- Frontend: `http://127.0.0.1:5173`

### 1.3 只重啟後端

```bash
./restart-backend.sh
```

---

## 2. 資料庫遷移（Alembic-only）

本專案目前採「只用 Alembic 管 schema」。

### 2.1 執行遷移

```bash
cd backend
alembic upgrade head
```

### 2.2 原則
- 上線前先跑 `alembic upgrade head`
- App 啟動不再自動 `create_all` 或 `ALTER TABLE IF NOT EXISTS`

---

## 3. 角色與主要頁面

### 3.1 admin
- Functions 管理：`/pages/functions.html`
- RAG 公有資料集管理：`/pages/rag-datasets.html`
- 代理者列表/設定：`/pages/agent-list.html`、`/pages/agent-llm-config.html?id=<agent_id>`

### 3.2 agent_admin
- 可在代理設定頁綁定 Functions 與 RAG（不可建立 Functions）

### 3.3 user
- 聊天頁：`/pages/chat.html`
- 不需手選代理者，系統會走主代理 Router 自動分派

---

## 4. Functions 操作

### 4.1 建立 Function Profile（admin）
1. 前往 `Functions` 頁
2. 點「新建 Function Profile」
3. 建議先套用模板範本（通用嚴謹版）
4. 儲存後可供代理者綁定

### 4.2 在代理者綁定 Function
1. 前往 `agent-llm-config.html`
2. 在「Functions 綁定」選擇 Function Profile
3. 儲存

### 4.3 匯入 Claude Skill（admin）

Claude Code 的 Skills 是提示詞模板，可直接匯入為 Function Profile。

1. 前往 `Functions` 頁
2. 點「匯入 Claude Skill」
3. 貼上 Claude Skill 內容（支援兩種格式）：

   **YAML front matter 格式：**
   ```yaml
   ---
   name: code-review
   description: Review code for quality
   prompt: |
     Review the following code for:
     1. Code quality
     2. Security vulnerabilities
   ---
   ```

   **純 Markdown 格式：**
   ```markdown
   # commit

   Create a git commit with a descriptive message.
   Follow conventional commit format...
   ```

4. 點「解析預覽」確認解析結果
5. 點「確認匯入」建立 Function Profile

### 4.4 覆寫規則優先序
1. `Functions 定義（進階）`（代理者覆寫）
2. 綁定的 Function Profile 模板
3. 系統預設模板

---

## 5. RAG 操作

### 5.1 公有資料集（global）
1. 前往 `RAG 資料集` 頁（`/pages/rag-datasets.html`）
2. 建立資料集
3. 在列表點「上傳文件」

### 5.2 私有資料集（agent_private）
1. 前往 `agent-llm-config.html`
2. 在「建立此代理者私有資料集」填資料後建立
3. 會自動加入本代理者綁定

### 5.3 私有文件上傳
1. 在 `agent-llm-config.html` 的「上傳代理者私有文件（RAG）」選檔
2. 點「上傳文件」
3. 觀察文件狀態（`uploaded/indexing/ready/failed`）

---

## 6. 聊天與 Router-Worker / Multi-Agent

### 6.1 統一入口
- API: `POST /api/chat`
- 一般使用者前端走統一入口，由主代理（Router）自動判斷分派策略

### 6.2 單代理路由策略（三層）

| 層 | 機制 | 成本 |
|----|------|------|
| 1 | 規則快速排除（訊息過短、可用 worker < 2） | 零成本 |
| 2 | Embedding 相似度（訊息 vs 代理者 name+description） | 低 |
| 3 | LLM 裁決（兩個代理距離相近時） | 有費用 |

路由使用的向量是 `代理者名稱 + 代理者說明（description）`。**說明寫得越精確，路由越準確**。

### 6.3 多代理協作（Multi-Agent Orchestrator）

當系統判斷使用者需求跨越多個代理者專長時，自動啟動多代理模式。

#### 觸發條件（依優先序）

| 條件 | 說明 | 範例 |
|------|------|------|
| **直接點名 2 個以上代理者名稱** | 最優先、最可靠 | 「請**前瞻研究部**設計 PoC，並請**MIS部門**估時程」 |
| **含複合動詞** + embedding 無明顯優勢 | 訊息含「同時」「另外」「也要」等詞，且無單一代理明顯佔優 | 「設計架構，**同時**列出風險清單」 |
| `@代理者名稱` 明確提及 | 用 @ 標記 | 「@MIS部門 評估資安風險」 |

> **不觸發**（仍走單代理）的情況：訊息雖含「同時」，但 embedding 距離某個代理明顯更近（gap > 0.2）

#### 執行流程

```
使用者訊息
  ↓
[主代理 Router] 判斷路由
  ↓ 多代理
[LLM 任務分解] → 產生子任務清單（最多 3 個）
  ↓
[Wave 執行] 各子任務分派給對應代理者（支援依賴順序）
  ↓
[Router 合成] 彙整各代理回覆 → 最終回答
  ↓ 自評 ok 或達上限
輸出結果（SSE 串流）
```

#### 前端 UI 說明

多代理模式啟動時，聊天介面會顯示：
- **routeInfo**（頂端）：從「正在分析任務分派…」→「多代理協作（第 N 輪）」→「多代理協作完成（N 個代理，共 M 輪）」（綠色）
- **協作計畫卡片**：可折疊的任務清單，含每個代理者的執行狀態徽章（待執行 / 處理中… / ✓ 完成 / ✗ 失敗）
- **子代理回覆**：各代理的輸出以可折疊區塊呈現
- **綜合回覆**：Router 合成的最終答案，以分隔線與「綜合回覆」標題醒目標示

#### 降級處理

若 LLM 任務分解失敗或超時（30 秒），自動降級為單代理，由主代理直接回答。

### 6.4 代理者設定對路由的影響

| 欄位 | 影響路由 | 說明 |
|------|---------|------|
| **名稱（name）** | ✅ 是 | 直接點名觸發多代理的關鍵；也參與 embedding 計算 |
| **說明（description）** | ✅ 是 | **最重要的路由線索** — embedding 相似度與 LLM 分解提示均使用此欄位。建議詳細描述代理者的專長與負責範疇 |
| **System Prompt** | ❌ 否 | 路由階段不讀取；只在代理者實際執行時引導其回覆風格與行為 |
| **模型設定（model_config）** | ❌ 否 | 路由不受影響；影響的是代理者執行時使用的 LLM |

> **建議**：代理者的「說明」應清楚列出其職責領域（如「負責 IT 系統規劃、軟體開發時程評估、資安政策」），避免與其他代理者的說明語義重疊，才能讓 embedding 距離拉開，路由更精準。

### 6.5 路由事件查詢
- `GET /api/conversations/{conversation_id}/routing-events`
- 可查看 `route.decision` / `route.forward` / `route.fallback`

---

## 7. Embedding 設定（.env）

可在 `backend/.env` 配置：

```env
EMBEDDING_PROVIDER=deterministic
EMBEDDING_MODEL_NAME=BAAI/bge-m3
EMBEDDING_OLLAMA_BASE_URL=http://localhost:11434
EMBEDDING_VLLM_BASE_URL=http://localhost:8000/v1
ROUTER_EMBEDDING_THRESHOLD=0.55
```

### 7.1 支援 provider
- `deterministic`：內建回退向量（開發可用）
- `sentence_transformers`：本機模型
- `ollama`：本地 Ollama embeddings API
- `vllm`：OpenAI 相容 embeddings API

### 7.2 常用範例

#### sentence_transformers
```env
EMBEDDING_PROVIDER=sentence_transformers
EMBEDDING_MODEL_NAME=BAAI/bge-m3
```

#### ollama
```env
EMBEDDING_PROVIDER=ollama
EMBEDDING_MODEL_NAME=bge-m3
EMBEDDING_OLLAMA_BASE_URL=http://localhost:11434
```

#### vllm
```env
EMBEDDING_PROVIDER=vllm
EMBEDDING_MODEL_NAME=BAAI/bge-m3
EMBEDDING_VLLM_BASE_URL=http://localhost:8000/v1
```

---

## 8. 除錯檢查清單

### 8.1 前端頁面 404
1. 確認 `frontend/vite.config.js` 已列入頁面 entry
2. 重新執行 `npm run build`
3. 重啟前端服務

### 8.2 聊天回覆「模型路由不可用」
1. 檢查後端 `.env` 的模型 provider / key
2. 檢查外部模型服務是否可連線
3. 確認被選中的代理者已在 `agent-llm-config.html` 設定好 LLM
4. 檢查 `/health` 與後端日誌

### 8.4 多代理模式不觸發（明明點名了兩個代理）
1. 確認代理者「名稱」欄位與訊息中出現的文字**完全一致**（如「MIS部門」≠「MIS 部門」有空格差異）
2. 只有一個 worker（系統只有主代理）→ 無法多代理，需建立至少 2 個 worker
3. 查看後端日誌是否出現 `orchestrator.decompose_failed`

### 8.5 多代理停在「正在分析任務分派…」很久
- 這是正常現象：LLM 任務分解需要 5–30 秒（取決於主代理使用的模型）
- 若超過 30 秒仍無回應，代表 LLM 分解逾時，系統會自動降級為單代理繼續回答
- 如需加速：在 `agent-llm-config.html` 將主代理的模型改為反應較快的選項

### 8.3 RAG 上傳成功但查不到
1. 確認文件狀態為 `ready`
2. 確認代理者已綁定對應資料集
3. 確認 Qdrant 服務正常

---

## 9. 驗證指令

### 9.1 後端測試
```bash
cd backend
pytest -q tests/unit/test_chat.py tests/unit/test_rag.py tests/unit/test_agents.py tests/unit/test_spec003_integration.py
```

### 9.2 前端建置
```bash
cd frontend
npm run build
```
